Two fixes needed:

## Fix 1: Move TabBar below template bar (node-hud-bar)
In ViewerPage.tsx, the TabBar is currently rendered ABOVE the template bar. Move it BELOW the template bar div. The order should be:
NodeHUD → Template bar → TabBar → Canvas/SplitGrid

Find where <TabBar> is rendered and move it after the template bar div. Look for "template-bar nodrag" to find the template bar.

## Fix 2: Per-tab chat message isolation
When switching between tabs, the AI chat panel shows the SAME messages for all tabs. Each tab should have its own independent chat history.

Implementation:
- In ViewerPage, add `chatStates: Record<string, Message[]>` state, keyed by tabId
- Pass the active tab's messages and a setter to AIChatPanel
- AIChatPanel receives `messages` and `onMessagesChange` props (when provided, use those instead of internal state)
- When tab switches, AIChatPanel shows that tab's messages

Files: frontend/src/features/viewer/ViewerPage.tsx, frontend/src/features/viewer/AIChatPanel.tsx
