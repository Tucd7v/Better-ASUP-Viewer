Fix two issues in ViewerPage.tsx SplitGrid component:

## Issue 1: Vertical resizing lost
After the 2-column CSS Grid refactor, the vertical (row) divider is gone. The old layout had both horizontal (vRatio) and vertical (hRatio) dividers for 2x2 grid. The new CSS Grid layout only has a vertical divider. Need to restore vertical resizing:
- Add a horizontal divider between rows for 2-column layout
- When row count > 2, add a draggable horizontal divider at vRatio position

## Issue 2: Layout logic
- 1 card: full width
- 2 cards: side by side with vertical divider (existing)
- 3 cards: 3 columns with 2 vertical dividers (restore from before)
- 4+ cards: CSS Grid 2 columns, auto-rows, with vertical divider between columns AND horizontal dividers between rows

### Implementation plan
Restore the cardCount-specific layouts for 1, 2, 3 cards from the original code. For 4+ cards (up to SPLIT_GRID_MAX_CARDS=8), use the new CSS Grid with 2 columns.

For the 4+ grid layout:
- Keep CSS Grid with `grid-template-columns: repeat(2, 1fr)` 
- Add horizontal dividers between row pairs using absolute positioning or CSS
- Keep the vertical column divider
- Vertical divider: split between left and right columns
- Horizontal dividers: split each row pair

File: frontend/src/features/viewer/ViewerPage.tsx
