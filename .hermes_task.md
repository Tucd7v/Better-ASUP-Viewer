Restore the original cluster/node logic while keeping the current Brocade-style visuals.

## ManagerPage.tsx — restore matchedClusterIds + autoExpand
- Restore `matchedClusterIds` useMemo (same logic as before: filter clusters by node hostname/SN/model_name match)
- Pass `autoExpand={matchedClusterIds.has(cluster.id)}` to ClusterCard

## ClusterCard.tsx — restore collapsed default + autoExpand
- Restore `autoExpand` prop
- Default `expanded=false` (not true)
- Restore autoExpand useEffect: if autoExpand is true, auto-expand after mount
- Remove the inline node table inside expanded area (hostname/model/serial table)
- Keep all current styling (colors, fonts, glass effect, rounded borders)
- Keep SessionRow and GroupRow as they are

## Keep as-is
- Glass header, DatabaseIcon, DM ASUP Analysis Tool text
- Hero section (h2 + subtitle)
- Filter bar styling
- All color tokens, fonts, glassStyle
