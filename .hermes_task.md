Fix NodeHUD for multi-node groups:

1. Remove the `.slice(0, 2)` limit — show ALL nodes in the group
2. When there are > 2 nodes, merge all node info into a single vertical column layout (dot + hostname + serial per row) instead of two horizontal bars
3. When ≤ 2 nodes, keep the current horizontal inline layout
4. Fix color usage: nodeColor is now a hex string (e.g. '#3b82f6') coming from the new palette, not a key into a color map. Use `session.nodeColor` directly as `backgroundColor`, not `NODE_COLORS[session.nodeColor]`.
5. Keep Cluster label and ASUP time display as-is

File: frontend/src/features/viewer/NodeHUD.tsx
