Add hostname label to card headers in all 3 card components (TextFileCard, EMSFileCard, XMLFileCard).

Current card header: [collapse] filename [×]

New card header: [collapse] filename | hostname [×]

Where:
- `|` is a vertical divider (1px solid, #e2e8f0, 12px height)
- `hostname` is the node hostname from the session this file belongs to

The hostname should come from existing data. Each card already receives props with fileId and sessionId. Use ViewerContext/ViewerPage state to look up the session's hostname by sessionId.

Files to modify:
- frontend/src/features/viewer/nodes/TextFileCard.tsx
- frontend/src/features/viewer/nodes/EMSFileCard.tsx  
- frontend/src/features/viewer/nodes/XMLFileCard.tsx

Style the hostname: font-size 11px, color #94a3b8, truncate with ellipsis, max-width 120px
