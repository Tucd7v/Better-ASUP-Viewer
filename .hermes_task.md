Fix HA pair line in FileTree: each paired node shows a blue `.ha-pair-line` (width 2px, background #3b82f6) but it only covers that single node's height. The lines should connect TWO paired nodes visually.

Currently:
```
[node A]  | blue line covering node A only
[node B]  | blue line covering node B only
```

Should be:
```
[node A]  ┐
          │ bracket connecting both nodes
[node B]  ┘
```

Idea: when two consecutive nodes are an HA pair, give the first one a top+bottom bracket opener and the second one a closer, so the vertical line spans both.

Or alternative: just make the line span BOTH nodes by adding CSS so the line from node A continues down to cover node B too.

The `haPairSessionIds` already correctly identifies which sessions are in a pair. In the render, determine if a node is the first or last in its consecutive pair, then apply appropriate CSS classes.

This is a frontend-only fix. Files: frontend/src/features/viewer/FileTree.tsx, frontend/src/index.css
