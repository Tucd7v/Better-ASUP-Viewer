Fix AI autonomous mode in NodeASUP viewer:

1. AIChatPanel mode toggle must be per-tab, not global. Add chatMode to Tab interface. View tabs default to 'analysis', AI tab defaults to 'autonomous'. Derive mode from activeTab.chatMode. Pass mode/onModeChange as props to AIChatPanel.

2. When AI is in autonomous mode on the AI tab, files it opens must appear as cards on the AI tab's canvas, not View 1. handleFocusFile already targets activeTabId via activeTabIdRef. Ensure this works correctly.

3. Switching from AI autonomous tab to a View tab must reset the mode toggle back to 'analysis'. Currently it stays on 'autonomous' and traps user.

Files: frontend/src/features/viewer/ViewerPage.tsx, frontend/src/features/viewer/AIChatPanel.tsx, frontend/src/features/viewer/TabBar.tsx
