Add cluster_name support throughout the project.

## 1. Parser: extract cluster_name from header
In `backend/services/asup_parser.py`, add to HEADER_FIELDS:
```
"X-Netapp-asup-cluster-name": "cluster_name",
```
Then extract it in the parse method (same pattern as serial_num, os_version, etc).

## 2. Database: add cluster_name column
In `backend/models/db.py`, add to Session model:
```
cluster_name: Mapped[str] = mapped_column(String, default="")
```
In `backend/core/database.py`, add auto-migration:
```
ALTER TABLE sessions ADD COLUMN cluster_name VARCHAR DEFAULT ''
```

## 3. Manager: show cluster_name as primary, uuid as secondary
In the Manager API response and frontend:
- API: include `cluster_name` in cluster response
- Frontend ClusterCard: show `cluster_name` as the main heading. If empty, fallback to first 8 chars of cluster_id. Below it, show the full cluster_id in smaller gray text (e.g. "UUID: 13cf3ffb-689c...")

## 4. Viewer FileTree: show cluster_name
In `frontend/src/features/viewer/FileTree.tsx`:
- The cluster header currently shows node info. Replace with cluster_name from the session data. If cluster_name is empty, fallback to cluster_id.
- No click-to-toggle in FileTree.

## 5. Viewer NodeHUD: clickable toggle
In `frontend/src/features/viewer/NodeHUD.tsx`:
- The cluster section at the top shows cluster_name by default
- Add a small toggle button (🔄) next to it
- Click toggles between cluster_name ↔ cluster_uuid
- Use `useState` for local toggle state

## 6. Backend: expose cluster_name in APIs
- `GET /api/v1/clusters` response: include `cluster_name`
- `GET /api/v1/sessions/{id}/status`: include `cluster_name`
- Any other endpoint that returns cluster info

## 7. Types
Add `cluster_name: string` to relevant TypeScript types/interfaces.

Files:
- backend/services/asup_parser.py
- backend/models/db.py
- backend/core/database.py
- backend/api/manager.py
- backend/schemas/api.py
- frontend/src/features/manager/ClusterCard.tsx
- frontend/src/features/viewer/FileTree.tsx
- frontend/src/features/viewer/NodeHUD.tsx
- frontend/src/types/index.ts
