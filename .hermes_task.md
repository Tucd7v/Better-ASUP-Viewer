Restore ColSwapMenu in XMLFileCard alongside the existing lock button.

## What was removed (commit 10eaef2):
- ColSwapMenu component: popup showing all available columns, clicking one replaces the current column
- allColumns state (Set<string>): full list of available columns
- swapMenuCol, swapMenuPos, wrapperRef, thRefs, handleSwap, openSwapMenu

## What to add back:
1. Restore the ColSwapMenu component (see below)
2. Add back allColumns state
3. Add back swapMenuCol, swapMenuPos, wrapperRef, thRefs
4. Add back handleSwap and openSwapMenu functions
5. Place the swap button (⇄) to the LEFT of the 🔒 lock button in the th header

## Optimizations (changes from original):
- Menu drops DOWN (not up): remove `transform: 'translateY(-100%)'`, use `top` directly
- Close on outside click: keep the full-card backdrop that calls onClose
- Keep 🔒 lock button working as-is

## ColSwapMenu component to restore:
```
function ColSwapMenu({
  allCols, targetSlot, left, top, cardWidth, onSelect, onClose,
}: {
  allCols: string[]; targetSlot: string; left: number; top: number;
  cardWidth: number; onSelect: (col: string) => void; onClose: () => void;
}) {
  const MENU_WIDTH = 180
  const clampedLeft = Math.min(left, cardWidth - MENU_WIDTH)
  return (
    <>
      <div style={{ position: 'absolute', inset: 0, zIndex: 9998 }} onMouseDown={onClose} />
      <div style={{
        position: 'absolute', top, left: clampedLeft, zIndex: 9999,
        background: '#fff', border: '1px solid #e2e8f0', borderRadius: 6,
        boxShadow: '0 4px 16px rgba(0,0,0,0.12)', width: MENU_WIDTH,
        maxHeight: 240, overflowY: 'auto',
        fontFamily: 'ui-monospace, Consolas, monospace', fontSize: 11,
      }} onMouseDown={e => e.stopPropagation()} className="nowheel nodrag">
        {allCols.map(col => (
          <div key={col} onClick={() => { onSelect(col); onClose() }}
            style={{
              padding: '6px 12px', cursor: 'pointer',
              color: allColumns.includes(col) ? (col === targetSlot ? '#2563eb' : '#334155') : '#94a3b8',
              background: col === targetSlot ? '#eff6ff' : 'transparent',
              fontWeight: col === targetSlot ? 600 : 400,
            }}
            onMouseEnter={e => { if (col !== targetSlot) e.currentTarget.style.background = '#f1f5f9' }}
            onMouseLeave={e => { if (col !== targetSlot) e.currentTarget.style.background = 'transparent' }}
          >{col}</div>
        ))}
      </div>
    </>
  )
}
```

## Header button layout per th:
```
[col name ↑↓] [⇄] [🔒]
              ^ swap button on left of lock
```

Place both buttons absolutely: swap at right: 24px (before lock), lock at right: 4px.

File: frontend/src/features/viewer/nodes/XMLFileCard.tsx
