Debug: Portal upload detects DM ASUP as UNKNOWN type.

## Context
Local test environment running:
- Portal :3000 (API) + :5173 (Web)
- NodeASUP :5180 (DM backend)
- Code: /data/LoginPortal-Remote/LogAnalysis/

## Symptom
Upload a DM ASUP .7z file via Portal (:5173), the detection returns type UNKNOWN instead of DM.

## Files to check
1. `portal/server/src/routes/upload.ts` — was modified to add DM forwarding (lines 99-115). Check if the syntax is correct (no missing brackets/semicolons)
2. `portal/server/src/services/log-detector.ts` — `detectDM()` function checks for SYSCONFIG-A.txt. Verify the detection logic is called correctly
3. `portal/server/src/services/upload-service.ts` — check if extractArchive and scanFileTree produce the expected file entries

## Debug approach
1. First check Portal server logs: `curl http://localhost:3000/api/health` to ensure it's running
2. Check if there are syntax errors in the modified upload.ts
3. Upload a test file and watch the server console for `[Upload]` log messages
4. Fix any issues found

## Key points
- DM detection: `detectDM()` looks for `SYSCONFIG-A.txt` in extracted files
- The upload flow: multer → extract → detectLogType → if DM, forward to :5180
- Must return type='DM' not 'UNKNOWN'

Files at: /data/LoginPortal-Remote/LogAnalysis/