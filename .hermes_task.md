In SplitGrid (ViewerPage.tsx): if ALL visible cards are XML type AND cardCount >= 4, switch to single-column vertical layout (1 card per row, full width, up to 8 rows).

## Logic
```
const allXML = gridNodes.every(n => n.type === 'xmlFile')
if (allXML && cardCount >= 4) {
  // single column with N rows
  // use gridRowRatios for row dividers
  // each card expands to full container width
}
```

## Details
- 1-3 cards: keep existing 1/2/3 logic (XML or not)
- 4+ cards all XML: single column, each card full width, draggable horizontal row dividers
- 4+ cards mixed: default 2-column grid (existing behavior)
- When user drag-drops a TXT/EMS into grid: auto-switch back to 2-column
- When all TXT/EMS removed: auto-switch to single column

## Row divider behavior
- 4 cards → 3 horizontal dividers
- Reuse existing `startGridRowDrag` / `gridRowRatios` code
- Each divider is 100% width, 4px height, cursor: row-resize

## Initial row ratios
- Equal distribution: `Array(cardCount).fill(100 / cardCount)`
