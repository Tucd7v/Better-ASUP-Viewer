In SplitGrid (ViewerPage.tsx lines 201-440): when ALL visible cards are XML type, use single-column vertical layout for ALL card counts (1-8). No >=4 restriction.

## Logic
```ts
const allXML = gridNodes.every(n => n.type === 'xmlFile')
```

### If allXML:
- 1 card: full container (already works — don't touch)
- 2-8 cards: single column grid, each card full width, draggable horizontal row dividers
- Skip the existing 2-card flex / 3-card flex branches
- Fall through to the general grid code below

### If NOT allXML:
- Keep EXACTLY the existing behavior (1/2/3 flex branches + 4+ 2-column grid)

## Implementation
- Remove `isSingleColumnXMLGrid` variable
- Just use `allXML` directly
- In the 2-card and 3-card early-return branches: add `&& !allXML` to skip when pure XML
- The existing general grid code (below 3-card branch) already supports single column via `isSingleColumnXMLGrid` — replace that with `allXML`
- Build and verify: `npm run build` must pass
