In XMLFileCard.tsx, change column sort from clicking the entire <th> header to a dedicated button.

## Current state
- `<th onClick={() => handleSort(col)}>` — entire column header clicks sort
- Header has: col name, resize handle at right edge, swap button at right:4, lock button nearby

## Required change
Remove sort from header click. Add a small sort indicator button that does NOT conflict with column name text or other buttons.

### Layout (right to left in header):
```
[resize handle: right: -2, 4px]  [lock 🔒: right: 4]  [swap ⇄: right: 26]  [sort ▲/▼: right: 48]  [text centered]
```

### Implementation:
1. Remove `onClick={() => handleSort(col)}` from `<th>`
2. Remove inline `{sortIndicator(col)}` after `{col}` text
3. Add a `<button>` for sort at `position: absolute; right: 48` inside `<th>`
4. Show `▲` when sortCol===col && sortDir==='asc', otherwise `▼`
5. Color: blue (#2563eb) when active, gray (#94a3b8) when inactive
6. `className="nodrag"`
7. Don't change any other buttons (swap, lock, resize handle must stay in their positions)
8. Don't change the right-padding of th (currently 46px — keep it)
9. The sort button should have: background none, border 0, cursor pointer, fontSize 10, padding '0 2px'

File: frontend/src/features/viewer/nodes/XMLFileCard.tsx
