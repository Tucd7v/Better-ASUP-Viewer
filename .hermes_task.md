Add model_name extraction and display.

## Backend
1. `backend/services/asup_parser.py` — add to HEADER_FIELDS:
   `"X-Netapp-asup-model-name": "model_name",`
   Extract it in the parse method (same pattern as cluster_name).

2. `backend/models/db.py` — add to Session model:
   `model_name: Mapped[str] = mapped_column(String, default="")`

3. `backend/core/database.py` — auto-migration:
   `ALTER TABLE sessions ADD COLUMN model_name VARCHAR DEFAULT ''`

4. `backend/schemas/api.py` — add `model_name` to:
   - SessionStatusResponse
   - ClusterGroupMember
   - Any other relevant response types

5. `backend/api/upload.py` — expose model_name in session status response

6. `backend/api/manager.py` — include model_name in:
   - _member() function
   - cluster list node info

## Frontend
7. `frontend/src/types/index.ts` — add `model_name` to SessionMeta and ClusterGroupMember types

8. `frontend/src/features/viewer/ViewerPage.tsx` — pass model_name through session metadata

9. `frontend/src/features/manager/NodeRow.tsx` — show model_name next to hostname (small gray text, e.g. "FAS2750")

10. `frontend/src/features/viewer/NodeHUD.tsx` — show model_name in node info chips
