Fix search + date filtering in ManagerPage:

## 1. Replace dropdown with date picker
Remove the `<select>` date filter dropdown. Add a date range input: two `<input type="date">` (From – To). When both are set, filter clusters by `cluster.last_seen` falling within that range. Leave empty for no filter.

## 2. Fix search broken when "Only show Today" active
Current code: `const timeFilter = onlyToday ? 'today' : dateFilter`. The filter logic works but the UI issue is that clicking "Only show Today" should not disable the search input. Actually check if the search is still functional — it should be since `filtered = useMemo(() => clusters.filter(...), [clusters, search, timeFilter])`. If broken, fix it.

## 3. Auto-expand clusters when search matches
When a search query matches a node (hostname or serial_num), the matching cluster card should auto-expand to reveal the matched node. 

Implementation:
- Compute `matchedClusterIds` as a Set of cluster IDs that have matching nodes
- Pass `autoExpand` prop to ClusterCard: `autoExpand={matchedClusterIds.has(cluster.id)}`
- In ClusterCard, when `autoExpand=true`, set `expanded=true` via useEffect

File: `frontend/src/features/manager/ManagerPage.tsx`
File: `frontend/src/features/manager/ClusterCard.tsx` (add autoExpand prop)
