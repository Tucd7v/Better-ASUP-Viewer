Fix AI Summary UI in ViewerPage.tsx:

## 1. Button state
Make the 💡 AI Summary button show active/pressed state when the panel is open (same as AI button: blue background when active).

## 2. Sidebar panel instead of modal
Replace the floating AISummaryDialog modal with a sidebar panel that:
- Opens as a right-side panel, same width as AI panel (uses `aiPanelWidth`)
- Has a resize handle (like AI panel)
- Shows node sections with collapsible content (keep existing AISummarySection logic)
- Has a close button in the header
- Uses the same panel container pattern as AIChatPanel

## 3. Key changes
- Remove `showAISummary` state, use a single toggle
- Remove AISummaryDialog component (or keep in same file, just change to panel)
- Add divider + AI Summary button next to AI button in toolbar
- When clicked, show sidebar panel alongside (or instead of) AI Chat panel
- If both panels open: stack them or show one at a time (toggle closes the other)

## 4. Layout
```
[FileTree] | [Canvas] | [AI Summary Panel] 
```
Same as AI Chat panel layout.

## Verify
- Click 💡 → sidebar opens with node summaries
- Button stays active (blue) while open
- Resize handle works
- Close button works
