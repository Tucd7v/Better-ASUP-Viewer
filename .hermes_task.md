Add search + date filter to ManagerPage:

1. Search input: matches cluster ID, node hostname, and node serial_num (case-insensitive)
2. Date filter dropdown next to search: Today / 7 days / 30 days / All
3. "Only show Today" toggle button — when selected, deselects date filter and vice versa
4. All filtering is client-side via useMemo
5. Replace `clusters.map` with `filtered.map`

Current API: getClusters() already returns all clusters with nodes[].{hostname, serial_num}.

Implementation:
- Add `TimeFilter` type: 'today' | '7d' | '30d' | 'all'
- Parse cluster.last_seen as UTC+8 for date comparison
- `inRange(iso, days)` helper: Date.now() - parsed <= days*86400000
- `filtered = useMemo(() => clusters.filter(…), [clusters, search, timeFilter])`
- UI: flex row with search input + select dropdown
- Keep existing loading/empty states, just use `filtered` instead of `clusters`

File: frontend/src/features/manager/ManagerPage.tsx
