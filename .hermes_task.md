1. Rename CSS class "template-bar" to "toolbar" everywhere:
   - In ViewerPage.tsx: `<div className="template-bar nodrag">` → `<div className="toolbar nodrag">`
   - In index.css: `.template-bar` → `.toolbar`
   - Any other references

2. Move AI toggle button to toolbar:
   - The floating AI button in AIChatPanel (currently outside the panel, likely a sibling button at `#root > div > main > div:nth-child(4) > button`)
   - Move it into the toolbar div, AFTER the template load section, BEFORE the closing `</div>`
   - Add a divider line (`<div style={{ width: 1, height: 16, background: '#e2e8f0' }} />`) before the AI button
   - The AI button should be right-aligned (there's already a `flex: 1` spacer before the template section, so it will naturally flow to the right)
   - The AI button should toggle chatMode between 'analysis' and 'autonomous', using handleChatModeChange
   - Text: chatMode === 'analysis' ? '🔍 AI Analysis' : '🤖 AI Auto'
   - Style: border, background color change based on mode (autonomous = blue highlight)

3. Remove duplicate divider divs:
   - Inside the toolbar, remove the 7th and 8th child divs (two consecutive `<div style={{ width: 1, height: 16, background: '#e2e8f0' }} />` elements)
   - These are the divider lines between the Save button and the Load select

Key files: ViewerPage.tsx, index.css, AIChatPanel.tsx (remove floating button)
