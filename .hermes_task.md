Add a status bar to the viewer page, positioned at the bottom of `<main className="viewer-main">` just before the `</main>` closing tag (currently line ~1129).

Status bar content (left to right):
- Cards: current view card count = `nodes.length`
- Total: all tabs card count = `tabs.reduce((sum, t) => sum + t.nodes.length, 0)`  
- Edges: `edges.length`
- Memory: `performance.memory?.usedJSHeapSize` formatted as MB (e.g. "45.2 MB"), fallback to "—" in non-Chromium browsers
- Grid: show "Grid" in blue when splitMode is on
- Right side: active tab name `activeTab.name`

Style: 28px height, background #f1f5f9, top border 1px solid #e2e8f0, 11px monospace font, color #64748b, items separated by 16px gap, padding 0 16px.

Memory should update periodically - add a `useState` for memory and a `useEffect` with `setInterval` every 5 seconds to refresh it.

File: frontend/src/features/viewer/ViewerPage.tsx only
